// JavaScript for future interactivity (optional)
// For example, to open a modal for viewing health records
document.addEventListener('DOMContentLoaded', () => {
    const viewRecordsButton = document.querySelector('button');
    viewRecordsButton.addEventListener('click', () => {
        alert('Fetching medical records...');
    });
});
